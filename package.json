{
  "scripts": {
    "start": "vite",
    "build": "vite build",
    "deploy": "vite build && gh-pages -d dist"
  },
  "devDependencies": {
    "gh-pages": "^6.1.0",
    "three": "^0.158.0",
    "vite": "^5.0.3"
  }
}
